字符串.split(',')
split() 方法用于把字符串拼接成一个数组
const arr=['aa，bb，cc']
arr.split()  //什么都不写就是 'aa,bb,cc' 一个数组就这一个元素
arr.split(',')  //以，为元素分割点 就是 'aa，bb，cc' 一个数组有三个元素
其他加了什么就  'aa加了什么bb加了什么cc'  以加了什么作为每个数组元素的分隔
字符串是通过参数里面指定的分隔符进行分隔的