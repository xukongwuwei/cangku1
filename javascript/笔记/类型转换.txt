分为隐式转换和显式转换

隐式转换：
-  会把' '转换为0
null也当0看
用+字符串   +只能写在最前面

显示转换：
Number（数据或者变量）
转换成数字类型
如果是非数字，转换失败结果为NaN 即（Not aNumber）不是一个数字
NaN也是number类型的数据，代表非数字

parseInt（数据）
只保留整数

parseFloat（数据或变量）
可以保留小数

转换为布尔类型
Boolean( )