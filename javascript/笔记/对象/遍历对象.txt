for(let k in 对象名){
k  //是属性名  等同 k==='属性名'
对象名[k]  //属性里的值  等同对象名['属性名']
}
不用k++
for in语法中的 k 是一个变量, 在循环的过程中依次代表对象的属性名
由于 k 是变量, 所以必须使用 [ ] 语法解析
一定记住： k 是获得对象的属性名， 对象名[k] 是获得 属性值


一次循环只代表一个属性名
第一次循环是第一个属性名
第二次循环是第二个属性名
